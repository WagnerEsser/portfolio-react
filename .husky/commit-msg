yarn commitlint --edit "$1"
status=$?

if [ $status -ne 0 ]; then
  echo "✖ Commit message validation failed. Valid types are: feat, fix, refactor, style, docs, build, test, chore."
  exit 1
fi

exit 0